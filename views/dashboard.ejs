<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <link rel="stylesheet" href="/css/style.css"> <!-- CAMINHO CORRETO -->
</head>
<body>
  <header>
    <h1>Bem-vindo, <%= user.email %>!</h1>
    <nav>
      <a href="/contacts/new">➕ Novo Contato</a>
      <a href="/logout">🚪 Sair</a>
    </nav>
  </header>

  <main>
    <h2>Meus Contatos</h2>

    <% if (contacts.length === 0) { %>
      <p>Você ainda não tem contatos salvos.</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Nome</th>
            <th>Telefone</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
          <% contacts.forEach((contact) => { %>
            <tr>
              <td><%= contact.name %></td>
              <td><%= contact.phone %></td>
              <td>
                <a href="/contacts/edit/<%= contact.id %>">✏️ Editar</a>
                <form action="/contacts/delete/<%= contact.id %>" method="POST" style="display:inline;">
                  <button type="submit" onclick="return confirm('Tem certeza?')">🗑️ Excluir</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </main>
</body>
</html>