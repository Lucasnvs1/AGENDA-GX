<%- include('partials/header') %>
        <div class="form-container">
            <div class="form-header">
                <h1>✏️ Editar Contato</h1>
                <p>Atualize as informações do contato</p>
            </div>
            
            <form action="/contacts/update/<%= contact.id %>" method="POST" class="contact-form">
                <div class="form-group">
                    <label for="name">Nome completo</label>
                    <input type="text" id="name" name="name" value="<%= contact.name %>" required placeholder="Ex: João Silva">
                </div>
                
                <div class="form-group">
                    <label for="phone">Telefone</label>
                    <input type="text" id="phone" name="phone" value="<%= contact.phone %>" required placeholder="Ex: (11) 99999-9999">
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">💾 Atualizar Contato</button>
                    <a href="/dashboard" class="btn" style="background: var(--gray-200); color: var(--gray-700);">↩️ Voltar</a>
                </div>
            </form>
        </div>
<%- include('partials/footer') %>